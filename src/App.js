// onChange = event handler used primarily with form elements: <input>, <textarea>, <select>, <radio>. Triggers a function every time the value of the input changes

import MyComponent from "./MyComponent";

function App() {
  return (
    <MyComponent />
  );
}

export default App;
